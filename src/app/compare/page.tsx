import { ComparePage } from "@/components/compare/ComparePage";

export const metadata = {
  title: "Compare Colleges — Side by Side | Campass",
  description: "Compare Indian colleges side-by-side on fees, placements, ratings and more.",
};

export default function CompareRoute() {
  return <ComparePage />;
}
